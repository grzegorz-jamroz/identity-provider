services:
  identity-provider:
    container_name: ${APP_CONTAINER_NAME}
    build:
      context: .
      dockerfile: docker/Dockerfile
    environment:
      - DB_HOST=${DB_HOST}
    restart: unless-stopped
    volumes:
      - ./src:/app/src
    networks:
      - web

networks:
  web:
    external: true
